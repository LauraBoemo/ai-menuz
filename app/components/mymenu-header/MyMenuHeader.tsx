import { Typography } from "@mui/material";

export const MyMenuHeader = () => {
  return ( 
    <Typography variant={"h1"} color={"primary.light"}>My Menu</Typography>
  );
}

export default MyMenuHeader;
